# 项目工作流约束

## 工作流

- 默认按 `brainstorm -> plan -> execute -> review -> compound` 推进
- `brainstorm` 只在需求不清、范围未定、方案分叉或未知项较多时启用
- 需求已经清晰时，直接进入 `plan`
- `docs/prompts/*.md` 是给 Codex 复用的参考提示词，不假设任何隐藏命令或专用 runtime

## 产物目录

- `docs/brainstorms/`：需求澄清与方案收敛
- `docs/plans/`：正式执行计划
- `docs/reviews/`：重要改动的复核与验证记录
- `docs/solutions/`：可复用决策、坑点和经验沉淀
- `docs/prompts/`：可直接复制或改写给 Codex 的参考提示词
- `docs/*/TEMPLATE.md` 只作结构参考；正式内容优先写入同目录下的具体文件，例如 `docs/plans/YYYY-MM-DD-short-name.md`

## 语言与路径

- `AGENTS.md`、`docs/` 下工作流文件、代码注释、说明文档、提交信息默认使用简体中文
- 函数名、类型名、API 名称、配置键、命令名、路径、协议字段等领域性标识保持英文，或沿用项目既有约定
- 文档内统一使用仓库相对路径

## 执行规则

- 纯信息型、小范围问答可直接回答，不强制创建文档
- 非微小任务开始前，先确认已有正式 plan；缺少 plan 时，优先补 `docs/plans/`
- 大任务必须先在 plan 中拆出阶段和执行单元，默认不要把整个大任务直接当成一个执行块
- `execute` 阶段按小步、可验证方式推进，完成一个闭环后先验证，再决定继续还是转入 `review`
- 实施过程中避免无关重构；如果发现范围扩张，先显式记录并确认
- 只有在缺关键决策、缺权限 / 凭证 / 外部输入、存在危险不可逆操作，或工作已经完成且验证通过时，才暂停推进

## Review

- 改动完成后对照 plan 复核结果
- 至少执行聚焦验证，并检查明显回归、范围漂移、遗漏和与计划不一致之处
- 需要保留复核结论时，写入 `docs/reviews/`

## Compound

- 出现关键决策、复发坑点、稳定排查路径或可复用模式时，写入 `docs/solutions/`
- 不把一次性执行日志原样沉淀进去，优先保留可复用结论

## 工作方式

- 优先做小而可验证的改动
- 尽量复用已有计划、审查或沉淀文档，不重复开平行文档
- 不引入隐藏脚本层、后台调度器或重型流程框架，除非任务明确需要
