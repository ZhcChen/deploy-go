---
date: 2026-08-01
topic: ui-interaction-refinement-plan
result: passed
plan: docs/plans/2026-08-01-ui-interaction-refinement.md
---

# UI 交互完善计划复核

## 结论

计划经一致性、可实施性和交互设计复核后通过。发现的执行顺序、导航守卫、敏感信息、筛选播报、检查依赖和移动返回问题均已修正，当前计划可以进入实施。

## 已处理发现

- 将 Playwright runner、锁文件、隔离预览进程和 `make ui-test` 从 U6 前移到 U1，保证每个执行单元完成后都能运行聚焦交互回归。
- 明确 hash 路由无法直接取消时的 dirty form 守卫协议，区分站内导航、浏览器历史与离开文档。
- 区分敏感引用元数据与 secret 内容：允许受控引用名称和脱敏摘要，禁止密码、私钥、secret 内容及完整敏感文件路径进入持久状态、日志或页面。
- 为搜索、组合筛选、清空、加载更多和加载失败增加可见摘要与持久 `aria-live` 结果播报要求。
- 枚举节点检查、应用契约校验和部署目标契约校验各自的依赖字段及检查失效条件。
- 为全部现有 App 二级路由确定稳定父页，并区分路由完成后的主内容聚焦与 Modal 关闭后的触发控件恢复。
- 修正完成定义，不再错误要求登录、用户创建、系统设置和个人资料等表单具备依赖检查失效。

## 范围复核

- 本轮只完善 `ui/` 已有页面与流程交互。
- 不增加 SSH 密钥管理或普通用户应用授权页面。
- 不创建正式 `admin/` 或 `admin-app/` 工程，不连接真实 API、SSH 节点或部署执行环境。
- Playwright 只作为设计源回归工具，不改变纯 HTML、CSS 和 JavaScript 的运行方式。

## 验证

- 计划 R1-R18、U1-U6 与 KTD1-KTD9 编号连续且引用可解析。
- 所有文件引用使用仓库相对路径。
- U1 提供后续单元需要的测试运行基线，不再存在验证向后依赖。
- `git diff --check` 通过。
