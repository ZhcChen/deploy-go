# 用途: 提供 Redis AOF、快照和数据目录基础配置。
# 推荐调整: 按数据耐久性要求调整 appendfsync/save，并在评估内存后补充模板允许的限制参数。
# 默认关系: dir=/data 对应 compose.yaml 的 redis-data 数据卷，默认开启 AOF everysec。
# 安全边界: 密码只在 redis.env 管理；不得在此引用模板目录外文件、模块或可执行命令。
appendonly yes
appendfsync everysec
dir /data
save 900 1
save 300 10
save 60 10000
