# 用途: 提供 Redis 访问密码和容器时区。
# 推荐调整: 部署前必须生成独立强密码，按业务地区修改 TZ。
# 默认关系: REDIS_PASSWORD 同时供启动命令和健康检查使用；TZ 默认 Asia/Shanghai。
# 安全边界: REDIS_PASSWORD=change-me 不可部署，文件必须由 Deploy Go 加密保存且不得提交仓库。
REDIS_PASSWORD=change-me
TZ=Asia/Shanghai
