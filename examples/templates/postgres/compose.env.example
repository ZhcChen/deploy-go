# 用途: 提供 PostgreSQL Compose 层的宿主端口和时区变量。
# 推荐调整: 部署前按节点端口占用修改 POSTGRES_PORT，按业务地区修改 TZ。
# 默认关系: POSTGRES_PORT 与 compose.yaml 的宿主端口占位符对应，默认 5432；TZ 默认 Asia/Shanghai。
# 安全边界: 复制为 compose.env 后由 Deploy Go 加密保存，禁止提交正式值或增加未声明变量。
POSTGRES_PORT=5432
TZ=Asia/Shanghai
