# PostgreSQL 模板基础配置；正式环境请按节点规格和备份策略调整。
listen_addresses = '*'
max_connections = 100
shared_buffers = 128MB
dynamic_shared_memory_type = posix
max_wal_size = 1GB
min_wal_size = 80MB
log_timezone = 'Asia/Shanghai'
timezone = 'Asia/Shanghai'
