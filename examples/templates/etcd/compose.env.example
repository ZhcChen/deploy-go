# 用途: 提供单节点 etcd Compose 层的本机 client 端口和时区。
# 推荐调整: 仅在本机端口冲突时修改 ETCD_CLIENT_PORT，按业务地区修改 TZ。
# 默认关系: ETCD_CLIENT_PORT 与 compose.yaml 的 127.0.0.1 端口映射对应，默认 2379。
# 安全边界: 必须保持回环绑定；文件由 Deploy Go 加密保存，禁止提交正式值或增加未声明变量。
ETCD_CLIENT_PORT=2379
TZ=Asia/Shanghai
